spring:
  application:
    name: orderservice

  datasource:
    url: jdbc:mysql://localhost:3306/zeptodb
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

